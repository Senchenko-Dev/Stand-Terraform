- name: KAFKA | Get secrets
  hosts: all
  tasks:
    - name: Import secrets
      include_vars: "{{ vault_file }}"
      when: vault_file is defined
      tags: always
      delegate_to: localhost

- name: KAFKA | Run ext-kafks
  import_playbook: "{{ playbook }}"