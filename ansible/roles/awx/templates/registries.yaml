mirrors:
  docker.io:
    endpoint:
      - "{{docker_registry}}"
  "{{socket}}":
    endpoint:
      - "{{docker_registry}}"
  quay.io:
    endpoint:
      - "{{docker_registry}}"
  gcr.io:
    endpoint:
      - "{{docker_registry}}"
